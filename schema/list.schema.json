{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"href": "https://example.com/schemas/list.schema.json",
	"title": "List Schema",
	"description": "This schema is used for listing out related data.",
	"type": "object",
	"anyOf": [{
		"$ref": "https://example.com/schemas/base.schema.json"
	}],
	"properties": {
		"listItems": {
			"type": "array",
			"items": { "type": "string" }
			},
		"required": ["listItems"]
	}
}
